%TAG ! tag:heaven-lang.org,1.0:
---
title: Sample API
version: v1
baseUri: https://{host}.sample.com/{path}
uriParameters:
    host:
        name: Host
        type: string
    path:
        name: Path
        type: string
/media:
    name: Media
    get:
        name: retrieve
        body:
            application/json:
                schema: |
                    {
                         "$schema": "http://json-schema.org/draft-03/schema",
                         "properties": {
                             "input": {
                                 "required": false,
                                 "type": "string"
                             }
                         },
                         "required": false,
                         "type": "object"
                    }
                example: '{ "input": "hola" }'

