name: Unsecured
description: |
  For the most part, Instagram’s API only requires the use of a client_id. A 
  client_id simply associates your server, script, or program with a 
  specific application.
requires: 
  - &method get
provides:
  *method:
    query:
      client_id:
        description: |
          Client ID
        type: string
        example: CLIENT_ID
        required: true    